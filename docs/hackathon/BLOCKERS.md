# ShopVoice — Blockers

Only items that truly need the owner (anh Sơn) or an external party. Each has a workaround so work continues.

| # | Blocker | Needs | Workaround in place | Status |
|---|---|---|---|---|
| B1 | `pre-hackathon-baseline` tag could not be pushed from the agent session (git proxy allows only the session branch). | Nothing. | Created on GitHub by `.github/workflows/create-tag.yml` ([run 36212550108](https://github.com/vansyson1308/groceryclaw/actions/runs/36212550108)); the tag points at `a9f3cdb`. | resolved 2026-09-26 |
| B2 | No working AWS credentials in the agent environment (the environment's AWS keys are placeholders; STS, Bedrock and Polly all return `InvalidClientTokenId`). | Owner: IAM credentials (Bedrock InvokeModel on an Amazon Nova model in us-east-1, Polly SynthesizeSpeech, and deploy rights for CDK: CloudFormation, ECR, App Runner/ECS, RDS/EC2, IAM role creation), plus the $150 credits. | `BedrockClient` / `SpeechClient` interfaces with deterministic fakes; all tests are green on the fakes; CDK app is synthesised but not deployed. | open |
| B3 | Pre-existing on `main`: `npm test` was red and hung, so `v2-ci` timed out. Cause: stale Zalo-era tests (removed `verifyWebhookRequest`, old `/webhooks/zalo` route, `zalo_users` renamed by 012), and canary/admin tests that leaked child servers after a failed assertion. | Nothing. | Fixed on this branch: commits `bbff291`, `95a191e` and `c3259a1` port the tests and CI gates to the Telegram gateway, add a spawn harness that always stops children, and run the DB test files serially. GitHub `v2-ci` is green on `4a62424` (https://github.com/vansyson1308/groceryclaw/actions/runs/36210790866). | resolved |
| B4 | Alexa+ Preview developer access unknown. | Owner: confirm whether the account has Alexa+ MCP Preview access. | Simulated Alexa+ web app (`apps/alexa-sim`), which the rules allow. | open |
| B6 | The public `kiotviet-mcp` repo must exist before code can be pushed. The agent's GitHub integration gets `403 Resource not accessible by integration` on repo creation. | Owner: create an **empty** public repo `vansyson1308/kiotviet-mcp` (no README, license or .gitignore), then tell the agent. Optionally open the drafted SDK example PR (`docs/hackathon/oss/typescript-sdk-example/PR.md`). | The history is split into a local branch `kiotviet-mcp-export` (`git subtree split --prefix=oss/kiotviet-mcp`), ready to push as `main`, including its own CI workflow. | waiting on owner (one click) |
| B5 | MIT license and public repo need owner consent. | Owner: make the repo public (GitHub → Settings → General → Danger Zone → Change visibility). The agent's GitHub tools cannot change visibility. | Owner confirmed MIT on 2026-09-26. The root `LICENSE` has been added, and the full git history was scanned for secrets before going public: no real keys or tokens were found. | license done; visibility is the owner's click |
