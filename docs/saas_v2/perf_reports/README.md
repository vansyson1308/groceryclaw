# V2 Performance Reports

This directory stores generated load-test reports from `npm run load:light` and `npm run load:full`.

- `*.json`: parsed machine-readable metric snapshots.
- `*.md`: human-readable SLO evaluation reports.

Reports are timestamped as `YYYYMMDD_hhmm` in UTC.
